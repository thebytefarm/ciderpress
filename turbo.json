{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "inputs": ["src/**", "rslib.config.ts", "package.json"],
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "inputs": ["src/**", "tsconfig.json", "package.json"],
      "dependsOn": ["^build"]
    },
    "test": {
      "inputs": ["$TURBO_DEFAULT$", "src/**", "test/**", "vitest.config.ts"],
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "//#docs:dev:headless": {
      "cache": false,
      "persistent": true
    },
    "docs:build": {
      "inputs": ["docs/**", "apps/**", "packages/**", "ciderpress.config.*", "package.json"],
      "outputs": [".ciderpress/dist/**"],
      "dependsOn": ["^build"],
      "env": ["CIDERPRESS_BASE"]
    },
    "generate:schema": {},
    "//#lint": {
      "dependsOn": ["^typecheck"]
    },
    "//#format": {
      "dependsOn": ["^typecheck"]
    },
    "//#knip": {},
    "//#docs:generate": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "clean": {
      "cache": false
    }
  }
}
