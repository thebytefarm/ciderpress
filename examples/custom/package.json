{
  "name": "example-custom",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "portless": "acme",
  "scripts": {
    "setup:portless": "zx scripts/setup.ts",
    "predev": "zx scripts/setup.ts --check --quiet",
    "dev": "ciderpress dev",
    "docs:dev": "ciderpress dev",
    "docs:build": "ciderpress build",
    "docs:serve": "ciderpress serve"
  },
  "dependencies": {
    "ciderpress": "workspace:*"
  },
  "devDependencies": {
    "zx": "^8.8.5"
  }
}
