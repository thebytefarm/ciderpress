{
  "name": "benchmarks",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest bench",
    "bench:run": "vitest bench --run"
  },
  "dependencies": {
    "@ciderpress/config": "workspace:*"
  },
  "devDependencies": {
    "@codspeed/vitest-plugin": "^5.7.1",
    "vitest": "catalog:"
  }
}
